#include<stdio.h>
#include<string.h>

int countLength(char arr[]);

int main() {
    char name[] = "Limon";
    int length = strlen(name);
    printf("The length is: %d", length);

    return 0;
}
